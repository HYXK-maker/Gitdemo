# 服务端口
server.port=8081

# 队友数据库服务的地址（默认 8080 端口）
team.database.service.url=http://localhost:8080

# 以下 MyBatis 配置暂时保留（虽然排除了数据源，但不影响）
mybatis.mapper-locations=classpath:mappers/*xml
mybatis.type-aliases-package=com.teamdoc.backend.mybatis.entity

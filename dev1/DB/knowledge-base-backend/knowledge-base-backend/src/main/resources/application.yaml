server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/doc_system?useUnicode=true&characterEncoding=utf8&serverTimezone=GMT%2B8&useSSL=false
    username: root
    password: s10074511
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.doc.system.entity